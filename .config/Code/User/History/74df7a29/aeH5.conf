
plugin {
    borders-plus-plus {
        add_borders = 2 # 0 - 9

        # you can add up to 9 borders
        col.border_1 = rgb(7a4d46)
        col.border_2 = rgb(8ebaa4)

        # -1 means “default” as in the one defined in general:border_size
        border_size_1 = 2

        # makes outer edges match rounding of the parent. Turn on / off to better understand. Default = on.
        natural_rounding = yes
    }   

    hyprbars {
        
        bar_height = 32
        bar_color = rgba($backgrounde9)
        bar_blur = true

        bar_title_enabled = true
        bar_text_size = 12
        bar_text_font = SF Pro Display Semibold

        bar_text_align = center
        bar_buttons_alignment = left

        bar_padding = 15
        bar_button_padding = 6

        col.text = rgba($secondarye6)

        hyprbars-button = rgb(f85656), 15, , hyprctl dispatch killactive
        hyprbars-button = rgb(f9bc4a), 15, ,~/.config/hypr/scripts/toggleworkspace.sh
        hyprbars-button = rgb(39c951), 15, , hyprctl dispatch fullscreen 1

        # cmd to run on double click of the bar
        on_double_click = hyprctl dispatch fullscreen 1
    }
}
windowrule = plugin:hyprbars:nobar,floating:0
